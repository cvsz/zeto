{
  "name": "@zeaz/zeto",
  "version": "1.1.0",
  "description": "Zeto — AI content factory and Facebook publishing automation dashboard",
  "main": "src/server.js",
  "private": true,
  "engines": {
    "node": ">=22 <23"
  },
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "build": "node --check src/server.js",
    "lint": "eslint .",
    "test": "node --test",
    "check": "npm run lint && npm test && npm run build"
  },
  "dependencies": {
    "axios": "^1.18.1",
    "dotenv": "^16.6.1",
    "express": "^4.22.2",
    "form-data": "4.0.6",
    "multer": "^2.2.0",
    "node-cron": "^4.6.0"
  }
}
